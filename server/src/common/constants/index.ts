export const MODEL_NAME = {
  USER: 'User',
};

export enum AuthType {
  EMAIL = 'email',
  GOOGLE = 'google',
}

export enum Role {
  USER = 'user',
  ADMIN = 'admin',
}

export enum Gender {
  MALE = 'Male',
  FEMALE = 'Female',
  OTHER = 'Other',
}
